SELECT *
FROM sys.server_principals sp
INNER JOIN sys.server_permissions spe
ON sp.

SELECT *
FROM sys.server_role_members

SELECT *
FROM sys.server_permissions

SELECT [name], sysadmin 
FROM syslogins
WHERE sysadmin = 1